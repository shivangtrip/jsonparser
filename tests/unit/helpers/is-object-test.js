
import { isObject } from 'json-parser/helpers/is-object';
import { module, test } from 'qunit';

module('Unit | Helper | is object');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isObject([42]);
  assert.ok(result);
});

